#include <iostream>
using namespace std;

int main () {

  int year;
  cin << "Enter a Year: ";
  cout >> year;

  boot isLeap = false;

  if (year % 400 == 0) {
    isLeap = true;
  } else if (year % 100 == 0) {
    isLeap = false; 
  } else if (year % 4 ==0) {
    isLeap true;
  }

  if (leap)
}
